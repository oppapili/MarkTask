# --- AI-DLC: per-user cursors (do not commit) ---
aidlc/active-space
aidlc/spaces/*/intents/active-intent

# --- AI-DLC: per-clone token, sessions, and machine-local runtime under aidlc/ ---
aidlc/.aidlc-*

# --- AI-DLC: per-intent runtime graph (also covers per-Bolt worktree fragments) ---
aidlc/spaces/*/intents/*/runtime-graph.json

# --- AI-DLC: recovery, hooks-health, stop-hook, sensors scratch ---
aidlc/spaces/*/intents/.aidlc-*
aidlc/spaces/*/intents/*/.aidlc-*

# --- Project: marktask ---
node_modules/
dist/
*.tsbuildinfo
.DS_Store
Thumbs.db
.env
.env.local
.env.*.local
bun.lockb
