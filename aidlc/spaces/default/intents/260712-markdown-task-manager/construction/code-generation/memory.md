<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is maintained by the orchestrator during stage execution. Add observations at the gate ritual, not by editing here directly.

## Interpretations
- 2026-07-12T14:00:00Z — code-generation は mode:subagent・per-unit（gate:false で各ユニット実装→reviewer→next、全ユニット後に gate:true 単一ゲート）。per-unit のプラン承認・完了ゲートは orchestrator が抑制（stage-level ゲート1回で全ユニットを承認）。実コードはワークスペース root（TS+bun, src/core|cli|mcp）。最初の unit(U-task-core) でプロジェクト雛形（package.json/tsconfig/lint/test 設定）も作る。Test Strategy=Standard（コア重点, team-practices）。

- 2026-07-14T14:13:20Z — U-query-search: 前セッションでコード生成本体（`src/core/query.ts`・`query.test.ts`、`repository.ts` に `list()`/`listArchived()` 追記、`index.ts` バレル更新）が生成済みで plan は全 [x]。本セッションは検証＋produces(`code-summary.md`)＋reviewer(§12a) で本ユニット iteration を完了。gate:false のため report-approve せず `next` で次ユニットへ。reviewer 判定=READY(iter1, Minor 2 のみ)。
- 2026-07-14T14:25:16Z — U-state-management: 未生成ユニットのため、plan 作成 → mode:subagent で aidlc-developer-agent に生成委譲 → 検証 → summary → reviewer(§12a) の完全手順で実施。生成物 `src/core/state-machine.ts`（transition/糖衣/complete 分岐＋依存インタフェース）・`state-machine.test.ts`(24)・`index.ts` 更新。`assertValidStatus` は task-model 再利用(DRY)。done 分岐は U-recurrence/U-subtasks 未生成のため依存性逆転(seam)で対応。検証 136 pass/0 fail・tsc/lint クリーン。reviewer=READY(iter1, Findings なし)。gate:false のため next で次ユニットへ。
- 2026-07-14T14:41:27Z — U-recurrence（最重要・最リスク）: plan→subagent 生成→検証→summary→reviewer。生成物 `src/core/recurrence.ts`(495行, parse/stepOnce/nextDue/RecurrenceEngine.rollForward/describe/stripCount/decrementCount)・`recurrence.test.ts`(73, 境界厚め)・`index.ts` 更新。RecurrenceEngine は state-machine の RecurrenceRoller を implements し CompleteOutcome を type-only import(seam 結線, complete 経由 smoke test 済)。barrel は parse→parseRepeat / describe→describeRepeat 別名。検証 209 pass/0 fail・tsc/lint クリーン。reviewer=READY(iter1, Minor3=コメント明確化のみ)。
- 2026-07-14T14:51:28Z — U-subtasks: plan→subagent 生成→検証→summary→reviewer。生成物 `src/core/subtasks.ts`(138行, parseParentRef/isBlockingStatus/childrenOf/canComplete純粋/asSubtaskGuard/SubtaskService)・`subtasks.test.ts`(45)・`index.ts` 更新。**同期 SubtaskGuard seam vs 非同期 I/O** を、純粋 canComplete(Task[]スナップショット)＋asSubtaskGuard ファクトリで解決(TaskService が repo.list を一度ロードして注入)。設計 GuardResult{ok}→seam 準拠 {blocked}(=!ok) に命名統一。循環保護 visited、多階層再帰、OQ-4(done/cancelled 非block)。検証 254 pass/0 fail・tsc/lint クリーン。reviewer=READY(iter1, Minor2=JSDoc)。
- 2026-07-14T15:07:59Z — U-cli（統合キーストーン）: plan→subagent 生成→検証→E2E スモーク→summary→reviewer。生成物 `src/core/task-service.ts`(共有 TaskService, 250行)・`src/core/config.ts`(writeConfig/setConfigValue 追記)・`src/cli/output.ts`(OutputFormatter, 207行)・`src/cli/index.ts`(commander CLI, 520行)・`index.ts`/`package.json`(bin) 更新・task-service.test(27)/output.test(25)。**completeTask** が snapshot→asSubtaskGuard＋RecurrenceEngine→complete を結線（recurrence/subtasks seam の実結線点）、guard-blocked 非永続化、R11 type 双方向整合。TaskService はコア共有(U-mcp が再利用)。CLI は薄いアダプタ。`run(argv)` で exit code テスト可能。検証 306 pass/0 fail・tsc/lint クリーン・E2E スモーク通過。reviewer=READY(iter1, Minor2=ドキュメント)。
- 2026-07-14T15:26:54Z — U-mcp（最終ユニット）: plan→subagent 生成→検証→smoke→summary→reviewer。生成物 `src/mcp/handlers.ts`(528行, TOOL_DEFS/dispatchTool/to*Json/errorToStructured)・`src/mcp/index.ts`(74行, 低レベル Server+stdio)・`handlers.test.ts`(50)・`task-service.ts` に `setRecurrence` 追加。低レベル Server API＋手動検証で zod 直接依存回避、@modelcontextprotocol/sdk@1.29.0 のみ追加。CLI と同一 TaskService 共有（挙動一致）。stdio ローカル非公開・認証なし・外部送信なし。検証 356 pass/0 fail・tsc/lint クリーン・stdio 起動 smoke OK。reviewer=READY(iter1, Minor2)。全ユニット完了→次 next で gate:true settle 想定。

## Deviations
- 2026-07-12T14:20:00Z — U-obsidian-interop（spec, Could）の code-generation は mode:subagent だが、成果物がランタイムコードでなく Obsidian 互換のサンプル/ドキュメント（Tasks.base・サンプルタスク・README 節）のため、開発エージェントへの委譲ではなくインラインで生成した。code の専門性を要さず、繰り返しの空戻り(subagent)を避け効率化するための意図的逸脱。他ユニット（実コード）は従来どおり subagent 委譲。
- 2026-07-14T14:12:00Z — U-query-search の code-generation body で subagent への再委譲を行わず、既存の前セッション生成コードを検証（tsc/lint/test）＋summary＋reviewer で完了した。理由=コードは生成済みでプラン全達成のため、再委譲は空戻り/churn を生む。検証中に node_modules 未インストールで test/tsc/lint が一時失敗→`bun install` で解消（コード修正は不要、112 pass に回復）。

## Tradeoffs
- 2026-07-14T14:35:00Z — U-recurrence の日付演算に date-fns@4.1.0 を追加（package.json 未記載だったが application-design ADR-5 で採用決定済み）。代替=raw Date だが月末 clamp/うるう年/週跨ぎ/月加算で誤りやすく、決定済みスタックに沿って導入。バージョンはピン留め（gray-matter 4.0.3 / yaml 2.7.1 と整合）。
- 2026-07-14T15:00:00Z — U-cli で commander@12.1.0 を追加（application-design で CLI=commander 決定済み・package.json 未記載だった）。ピン留め。color ライブラリは追加せず ANSI 内蔵（不要な依存を増やさない）。

## Open questions
